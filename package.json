{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas — cross-project knowledge indexer: kdb logs + Claude Code sessions + git + docs, searchable via UI/CLI/REST/MCP",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/indexer",
    "packages/api",
    "packages/mcp",
    "packages/cli",
    "packages/ui"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/indexer -w packages/api -w packages/mcp -w packages/cli",
    "build:ui": "npm run build -w packages/ui",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit -p tsconfig.lint.json && tsc --noEmit -p packages/ui/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "license": "MIT"
}
